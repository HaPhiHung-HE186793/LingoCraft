/**
 * Home page scaffold — M0.
 * Full UI implemented in T12 (responsive/a11y/design system).
 * Per spec §04.1: first screen shows two clear paths.
 */
export default function HomePage(): React.JSX.Element {
  return (
    <main>
      <h1>LingoCraft</h1>
      <p>Xưởng luyện ngôn ngữ cá nhân — đang xây dựng.</p>
      {/* UI thật sẽ được xây trong T12 */}
    </main>
  );
}
